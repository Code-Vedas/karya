# Karya::ActiveRecord

Active Record adapter support for Karya.

`karya-activerecord` provides the Active Record-facing adapter surface used by
Karya SQL deployments.

## Development

```bash
bundle install
bin/rspec-unit
bin/rspec-e2e
bin/rubocop
bin/reek
```

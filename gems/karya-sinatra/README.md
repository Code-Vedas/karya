# Karya::Sinatra

Sinatra integration for Karya with Sequel support.

`karya-sinatra` composes `karya`, `karya-sequel`, and the `karya-dashboard`
addon as the current Sinatra-side integration scaffold.

## Development

```bash
bundle install
bin/rspec-unit
bin/rspec-e2e
bin/rubocop
bin/reek
```

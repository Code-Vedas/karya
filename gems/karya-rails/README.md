# Karya::Rails

Rails integration for Karya with Active Record support.

`karya-rails` composes `karya`, `karya-activerecord`, and the `karya-dashboard`
addon as the current Rails-side integration scaffold.

## Development

```bash
bundle install
bin/rspec-unit
bin/rspec-e2e
bin/rubocop
bin/reek
```

#!/bin/sh
set -eu

if [ "${SCRIPT_DIR:-}" = "" ]; then
  SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
fi
. "${SCRIPT_DIR}/run-common"

while IFS= read -r suite_dir; do
  run_optional_yarn_script "${suite_dir}" "build"
done <<EOF
$(js_suites)
EOF
